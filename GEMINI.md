# Multi-Agent Orchestrator Gemini Context

Use this repository as a Gemini CLI extension/context for multi-agent coding orchestration.

When the user asks to split a large implementation into parallel worker agents, follow the workflow in the imported skill instructions below. Resolve this extension/repository directory as the skill root, then run its scripts from the target project.

@./SKILL.md
